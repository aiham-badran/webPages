section#booking.booking.section
  .container
    .booking__body
      .booking__content.box--center
        h2.booking__title أحجز موقعك
        form.booking__form.box--center
          .row 
            .date-input 
              label(for="booking-date") التاريخ
              input#booking-date(type="date", name="bookingDate") 
            .time-input 
              label(for="booking-time") الوقت
              input#booking-time(type="time", name="bookingTime")
            .number-selector 
              label(for="booking-number") العدد
              select#booking-number(name="bookingNumber")
                option(value="1") 1   
                option(value="2") 2   
                option(value="3") 3   
                option(value="4") 4   
                option(value="5") 5 
          button.btn.btn--gray احجز الان 